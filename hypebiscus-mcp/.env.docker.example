# ==========================================
# Docker Environment Configuration
# ==========================================
# This file is specifically for Docker deployments
# Copy to .env.docker and customize for your environment

# Required: Solana RPC Endpoint
# Use your own QuikNode, Helius, or public RPC endpoint
# For production, use a private RPC endpoint with higher rate limits
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Optional: Birdeye API Key
# Get a free API key at https://birdeye.so
# Leave empty to use Jupiter Price API as fallback
BIRDEYE_API_KEY=

# Optional: Default Pool Address
# This is the zBTC-SOL DLMM pool on Meteora
DEFAULT_POOL_ADDRESS=2onAYHGyxUV4JuYeUQbFwbKmKUXyTA9v5aKiDgZMyCeL

# Optional: Cache TTL (seconds)
# How long to cache pool metrics and prices
# Lower values = more up-to-date data but more API calls
# Higher values = better performance but potentially stale data
CACHE_TTL=30

# Optional: Request Timeout (milliseconds)
# Maximum time to wait for external API responses
REQUEST_TIMEOUT=10000

# Optional: Log Level
# Options: error, warn, info, debug
# Use 'debug' for troubleshooting, 'warn' or 'error' for production
LOG_LEVEL=info
